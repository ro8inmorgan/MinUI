diff --git a/platforms/libretro/Makefile b/platforms/libretro/Makefile
index 581ee84..d9db102 100644
--- a/platforms/libretro/Makefile
+++ b/platforms/libretro/Makefile
@@ -287,6 +287,24 @@ else ifeq ($(platform), miyoo)
 	PLATFORM_DEFINES := -DCC_RESAMPLER -DCC_RESAMPLER_NO_HIGHPASS
 	CFLAGS += -fomit-frame-pointer -ffast-math -march=armv5te -mtune=arm926ej-s
 	CXXFLAGS := $(CXXFLAGS) $(CFLAGS)
+# target platform tg5050 trimUI smart pro s
+else ifeq ($(platform), tg5050)
+	TARGET := $(TARGET_NAME)_libretro.so
+	CC = $(CROSS_COMPILE)gcc
+	CXX = $(CROSS_COMPILE)g++
+	AR = $(CROSS_COMPILE)ar
+	fpic := -fPIC
+	SHARED := -shared -Wl,-version-script=$(CORE_DIR)/link.T
+	PLATFORM_DEFINES := -DCC_RESAMPLER -DCC_RESAMPLER_NO_HIGHPASS
+	CFLAGS += -Ofast \
+	-fuse-linker-plugin \
+	-fno-stack-protector -fno-ident -fomit-frame-pointer \
+	-fmerge-all-constants -ffast-math -funroll-all-loops \
+	-mcpu=cortex-a55
+	CXXFLAGS += $(CFLAGS)
+	HAVE_NEON = 1
+	USE_DYNAREC = 1
+	LDFLAGS += -mcpu=cortex-a55 -Ofast -flto -fuse-linker-plugin
 # PS2
 else ifeq ($(platform), ps2)
 	TARGET := $(TARGET_NAME)_libretro_$(platform).a
